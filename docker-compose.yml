version: '3'

services:
  proxy-node-gateway:
    image: registry.tools.verify.govsvc.uk/eidas/proxy-node-gateway:latest
    build:
      context: .
      args:
        component: proxy-node-gateway
  proxy-node-translator:
    image: registry.tools.verify.govsvc.uk/eidas/proxy-node-translator:latest
    build:
      context: .
      args:
        component: proxy-node-translator
  stub-connector:
    image: registry.tools.verify.govsvc.uk/eidas/stub-connector:latest
    build:
      context: .
      args:
        component: stub-connector
  softhsm:
    image: registry.tools.verify.govsvc.uk/eidas/softhsm:latest
    build:
      context: ./softhsm
